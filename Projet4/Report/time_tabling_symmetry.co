solve<cp> {
   ...           
} using {
   label(lectureslot[1],1);

   forall(e in revent) by (lectureroom[e].getSize()) 
      tryall<cp>(r in rroom : lectureroom[e].memberOf(r)) by (r)
         label(lectureroom[e],r);

   forall(e in revent) by (lectureslot[e].getSize()) 
      tryall<cp>(t in timeslot : lectureslot[e].memberOf(t)) by (t)
         label(lectureslot[e],t);
}